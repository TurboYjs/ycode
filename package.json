{
  "version": "0.0.4",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "release": "bumpp package.json --commit --push --tag --all"
  },
  "devDependencies": {
    "bumpp": "9.1.1"
  }
}
